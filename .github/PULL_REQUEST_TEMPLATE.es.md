# Pull request

## Resumen

<!-- ¿Qué cambia y por qué? -->

## Comprobaciones

- [ ] `node scripts/check.js` pasa en local.
- [ ] Si has añadido un asset al shell, está en `FILES` de
      `sw.js` y has bumpeado `VERSION`.
- [ ] Si has modificado el shell de la app, has actualizado la
      documentación canónica correspondiente (`doc/es/`).
- [ ] Si has cambiado contenido de producto, los strings `es`/`en`
      están en sincronía.
- [ ] Has probado el flujo en al menos un navegador real de
      escritorio.

## Caption

<!-- opcional. Capturas o notas para el revisor. -->
